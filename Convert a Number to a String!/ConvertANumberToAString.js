const numberToString = (num) => {
    return String(num);
};

const numberToStrUsingTemplate = (num) => {
    return `${num}`;
};

const convertNumberByConcatenation = (num) => {
    return num + "";
};