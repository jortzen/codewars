const printArray = (array) => {
    return array.join(",");
};

const toStringArray = (array) => {
    return array.toString();
};

const mapAndJoin = (array) => {
    return array.map(String).join(",");
};