const greet = (name) => `Hello, ${name} how are you doing today?`;

const greetConcat = (name) => "Hello, " + name + " how are you doing today?";

const greetMethod = (name) =>
    "Hello, ".concat(name, " how are you doing today?");